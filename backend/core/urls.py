"""API URL routing. Endpoints are wired up in the API phase."""
from django.urls import path

app_name = "core"

urlpatterns = [
    # path("risk-scores/", views.risk_scores, name="risk-scores"),
    # ... added in Phase 6
]
